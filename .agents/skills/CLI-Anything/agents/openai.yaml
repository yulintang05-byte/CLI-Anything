interface:
  display_name: "CLI Anything"
  short_description: "Repo-specific patterns and workflows for CLI-Anything"
  default_prompt: "Use the CLI-Anything repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true